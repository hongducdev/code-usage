[package]
name = "bintest"
version = "0.1.0"
edition = "2021"

[dependencies]
reqwest = { version = "0.13.4", features = ["blocking", "json"] }
rusqlite = { version = "0.32", features = ["bundled"] }
